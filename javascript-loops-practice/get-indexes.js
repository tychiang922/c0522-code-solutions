/* exported getIndexes */
function getIndexes(array) {
  var arrayIndex = [];
  for (var indexNum = 0; indexNum < array.length; indexNum++) {
    arrayIndex.push(indexNum);
  }
  return arrayIndex;
}
