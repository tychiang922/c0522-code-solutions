/* exported Transaction */
/* want to create a transaction OOP object with type and amount
1) define a function
2) set this.type and amount to type and mount */
function Transaction(type, amount) {
  this.type = type;
  this.amount = amount;
}
