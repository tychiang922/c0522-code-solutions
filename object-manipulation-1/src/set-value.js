/* exported setValue */

/* want to add property and value to an object without returning it
- assign value to the key property of an object */
function setValue(object, key, value) {
  object[key] = value;
}
